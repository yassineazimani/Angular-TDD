import { Routes } from '@angular/router';
import { PagepostComponent } from '../app/components/pagepost/pagepost.component';

export const routes: Routes = [{ path: '', component: PagepostComponent }];
